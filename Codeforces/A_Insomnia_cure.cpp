#include <bits/stdc++.h>
using namespace std;

// Bruteforce
// int main() {
//   int k, l, m, n, d;
//   cin >> k >> l >> m >> n >> d;
//   int total = 0;
//   int arr[d]{0};
//   // for (int i = 0; i < d; i++) arr[i] = i + 1;
//   for (int i = 1; i <= d; i++) {
//     if (i % k == 0 || i % l == 0 || i % m == 0 || i % n == 0) arr[i - 1] =
//     -1;
//   }
//   for (int i = 0; i < d; i++) {
//     if (arr[i] == -1) total++;
//   }
//   cout << total;
// }

// Efficient

int main() {
  int k, l, m, n, d;
  cin >> k >> l >> m >> n >> d;
}